<% 
displayBook = function(title) {
  const fetch = (...args) =>
	import('node-fetch').then(({default: fetch}) => fetch(...args));
  //const fs = (...args) => import("fs").then(({default: fs}));
  
  //let bookData = fs.readFileSync("../../book.json");
  //console.log(bookData);

  fetch("book.json")
      .then(response => response.json())
      .then(data => console.log(data))
      .catch(err => console.log(err));
  let bookTitle = "";
  let book_desc = "";


  }
 %> 